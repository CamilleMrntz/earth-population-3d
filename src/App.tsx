import { EarthViewer } from "./components/earth/EarthViewer";

/**
 * Root app shell. Milestone 1: only the 3D Earth viewer (no species UI yet).
 */
export default function App() {
  return <EarthViewer />;
}
